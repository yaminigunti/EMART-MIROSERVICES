spring:
    cloud:
        config: 
          name: gateway-service
          discovery: 
            service-id: config
            enabled: true


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/